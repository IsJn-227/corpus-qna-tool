# Copy this file to .env and fill in the keys for whichever backend(s) you use.
# Then `export $(cat .env | xargs)` before running the CLI, or use a package
# like python-dotenv if you prefer.

ANTHROPIC_API_KEY=your-anthropic-key-here
OPENAI_API_KEY=your-openai-key-here
